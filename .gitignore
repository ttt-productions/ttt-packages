# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Production
dist/
packages/*/dist/
build/

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
Thumbs.db

# Build artifacts
*.tsbuildinfo
packages/*/*.tsbuildinfo

# npm
.npm

*.tgz

packages/*/*.tsbuildinfo

# if a build ever emits into src/, ignore it
packages/*/src/**/*.js
packages/*/src/**/*.d.ts
packages/*/src/**/*.map
ttt-packages-*.zip

# --- Microsoft PhotoDNA SDK / CSAM hashing material — CONFIDENTIAL, NEVER COMMIT ---
# Licensed-to-us Microsoft material (EdgeHashGeneration WASM / binaries / zips) must never
# enter a repo. The photodna-coverage.ts code IS committed (no SDK/hashes/keys); only the SDK
# ARTIFACTS are ignored. Keys live ONLY in Secret Manager.
Microsoft_PhotoDNA_SDK*/
**/Microsoft_PhotoDNA_SDK*/
*EdgeHashGeneration*
*EdgeHash*.wasm
photodna-sdk/
**/photodna-sdk/
