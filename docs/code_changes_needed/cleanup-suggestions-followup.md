# Cleanup Suggestions — Follow-up Backlog (ttt-packages)

Generated by the autonomous cleanup run (2026-06-03). These are items the run
identified but did **not** execute. `@ttt-productions/*` packages are consumed by
apps outside this run's scope, so **no package export was ever deleted** — every
export that looked unused is listed here for human verification instead. File
splits (even internal ones) are listed with a note on whether they would change
the package's public exports.

Nothing in this file has been applied. Safe edits made directly this run are
recorded in `docs/CLEANUP_PROGRESS.md`.

---

## Package-export dead-code candidates (NOT deleted — verify against all consumers)

These exports are unreferenced WITHIN ttt-packages but `@ttt-productions/*` is
consumed by apps outside this repo — verify against ALL consumers (incl. ttt-prod
and any other app) before removing.

- `packages/ttt-core/src/media/target-info.ts` — `HallLibraryCoverSquareTargetInfoSchema`,
  `HallLibraryCoverPosterTargetInfoSchema`, `HallLibraryCoverCinematicTargetInfoSchema`
  (all alias one base schema), and the `TuneTrackPhoto/Audio` + `TelevisionEpisodePhoto/Video`
  target schemas — unreferenced in-repo but exported; likely used by ttt-prod upload code.
- `packages/ttt-core/src/types/work-project.ts` — `GUILDMATE_USER_PUBLIC_FIELDS` — exported;
  primary consumer is likely the ttt-prod `computePublicGuildmateMirror` trigger.
- `packages/ttt-core/src/schemas/admin.ts` — `CheckoutNextImportantTaskInputSchema` /
  `CheckoutNextImportantTaskInput` — callable schema; consumer is ttt-prod.
- `packages/upload-core/src/utils/upload-store.ts` — `clearUploadSessionListeners` — only
  called internally by `removeUploadSession`; low confidence it has external callers.

Intentional test-seam exports (do NOT remove — they have no production callers by design):
`monitoring-core` `setMonitoringAdapter`/`resetMonitoringAdapter`; `moderation-core`
`__resetDefaultVisionClient`/`__resetDefaultVideoClient`; `rate-limit-core` factory `__reset()`;
`media-viewer` `MediaPreview` (back-compat alias for `MediaViewer`).

## File splits / extractions (NOT executed)

Each notes public-export impact. All these packages re-export through a barrel
(`src/index.ts` / subpath barrels), so a split keeps the public surface identical
*if the barrel is updated* — low API risk, but still a deliberate refactor.

- `packages/ttt-core/src/media/domain-events.ts` (~595 lines) — one Zod schema per
  domain-event type. Split by domain group (user / work / social / admin). Barrel
  re-exports → public API unchanged.
- `packages/ttt-core/src/permissions/work-project-permissions.ts` (~315) — combines
  `GUILD_STANDINGS` + `WORK_PROJECT_ACTIONS` data with the permission utility
  functions. Split data vs. logic. Barrel re-exports → API unchanged.
- `packages/ttt-core/src/constants/business.ts` (~250) — 15+ unrelated domain
  constant groups in one file. Split by domain. `constants/index.ts` barrel →
  API unchanged.
- `packages/report-core/src/types.ts` — mixes admin-task types, report types,
  config-prop types, and UI-prop types. Split along those four lines; re-export
  through `src/index.ts` + `src/contracts.ts` to preserve the published surface.

## Risky / non-local dead-code candidates (NOT deleted)

- `packages/media-processing-core/src/image/formats.ts` — file contains ONLY a
  comment (`// shared image format / quality presets`), no code. Not on the
  `image/index.ts` barrel; nothing imports it. Dead stub. (Delete, or fill in.)
- `packages/media-processing-core/src/image/resize.ts` — only two comments
  (`// sharp resize helpers extracted...` + `// intentionally empty until extraction
  step`). Dead stub / intentional placeholder for a planned extraction. Not on the
  barrel. (Either complete the extraction or delete.)
- `packages/mobile-core/src/react/keyboard/focusOrder.ts` (~line 5) — no-op ternary
  `const el = root instanceof Document ? root : root;` (both branches identical).
  Logic smell, not dead code — likely a refactor leftover; simplify to `const el = root;`.
- `packages/report-core/src/components/ReportDialog.tsx` (~line 60) — `_commentRef`
  (`useRef`, underscore-prefixed) is created but never attached to a `ref=` prop.
  Unused; underscore suggests it was parked for future use.

## Proposed canonical doc structures (NOT executed)

_None recorded yet._

## Other refactor suggestions

_None recorded yet._
