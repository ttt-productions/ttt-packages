{
    "name": "@ttt-productions/chat-schemas",
    "version": "0.10.0",
    "description": "The chat realtime wire contract (subprotocol, frame kinds, close codes, channel-ref tuple, grant scope, client-agreed limits) in pure Zod/TS. Tier 0 — consumed by the chat React client, the chat Cloudflare Worker, and Cloud Functions without forcing the chat UI dep graph on backend callers.",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/ttt-productions/ttt-packages.git",
        "directory": "packages/chat-schemas"
    },
    "type": "module",
    "main": "dist/index.js",
    "module": "dist/index.js",
    "types": "dist/index.d.ts",
    "sideEffects": false,
    "files": [
        "dist"
    ],
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "default": "./dist/index.js"
        }
    },
    "scripts": {
        "build": "tsc",
        "clean": "rm -rf dist *.tsbuildinfo",
        "typecheck": "tsc --noEmit",
        "prepublishOnly": "npm run clean && npm run build",
        "test": "vitest run"
    },
    "dependencies": {
        "zod": "^4.4.3"
    },
    "devDependencies": {
        "typescript": "^5.8.3"
    },
    "author": "DJ (TTT Productions)",
    "license": "MIT"
}
